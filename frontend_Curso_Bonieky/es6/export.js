export const criador = "Eduardo";

export function somar(x, y) {
    console.log(x + y);
    return x + y;
}
export function subtrair(x, y) {
    console.log(x - y);
    return x - y;
}

function multiplicar(x, y) {
    return x * y;
}